# Demo assets

`sample-telemetry.json` is included so the starter remains deterministic and usable without a camera.

Before Phase 1, add your own fixed front-view aquarium video here:

```text
public/demo/goldfish-demo.mp4
```

The video is intentionally excluded from Git by default because it may be large and may reveal private room details. Review the frame carefully before publishing it with the repository or demo video.
